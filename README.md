# practice

```shell
./index [problem_name]
```

## Dependencies

- Node(for fetching in/output samples)
- Visual Studio Code(as just a text editor)
- git(for diff engine)
- Google Chrome

## Install

```shell
npm install
```
